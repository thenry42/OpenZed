---
description: "Bootstrap .opencode directory structure with learnings and rules"
---

Create the `.opencode/` directory structure at the project root if it doesn't exist:

1. Create `.opencode/learnings/` if missing.
2. Create `.opencode/rules/` if missing.
3. If `.opencode/learnings/LEARNINGS.md` doesn't exist, create it with:

   ```markdown
   # Learnings

   Lessons learned the hard way, so we don't go in circles.
   ```

4. Report what was created vs. already existed.